server:
  port: 8080

spring:
  application:
    name: "mindease"
  datasource:
    url: jdbc:postgresql://localhost:5433/tech1_db
    username: user
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  mail:
    host: smtp.gmail.com
    port: 587
    username: assaa1122a@gmail.com
    password: vjqe zxen wvhk jizc
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  quartz:
    job-store-type: jdbc
    jdbc:
      initialize-schema: always
    properties:
      org:
        quartz:
          scheduler:
            instanceName: MindEaseScheduler
          threadPool:
            threadCount: 5