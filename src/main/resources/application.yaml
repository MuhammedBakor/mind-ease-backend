server:
  port: 8080

spring:
  application:
    name: "mindease"
  datasource:
    url: jdbc:postgresql://localhost:5433/tech1_db
    username: user
    password: password
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
